$(function() {
    // // events    
    // $(document).on("click", ".SendMsg", function(e) {
    //     var id = $(this).attr("data-room-id");
    //     var text = $('#ch-input-' + id).val();
    //     new SendMsg({room: new ChatRoom(id), msgText: text}).fire(); 
    // });
    
    // $(document).on("click", ".JoinRoom", function(e) {
    //     var id = $(this).attr("data-room-id");
    //     new JoinRoom({room: new ChatRoom(id)}).fire(); 
    // });
});

// Red.onEventSent = function(response) {
    
// };

// Red.onEventFailed = function(response) {
//     alert(response.error);
// };
